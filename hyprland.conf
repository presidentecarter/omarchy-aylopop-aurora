# Aylopop Aurora – Hyprland theme bits (no shadows per Omarchy)
general {
    col.active_border = rgb(22D3C5)
    col.inactive_border = rgb(0F1F29)
}

decoration {
    rounding = 8
    inactive_opacity = 0.97
    # drop_shadow intentionally omitted
    # shadow_range omitted
}

# Slight transparency for Alacritty
# windowrule = opacity 0.98 0.95, class:Alacritty
